# ── PyTorch (install CUDA version separately via conda before this) ──────────
# conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia

# ── Core ML models ────────────────────────────────────────────────────────────
transformers>=4.45.0
accelerate>=0.33.0
kokoro>=0.9.2
torch>=2.3.0          # if not installed via conda above

# ── Audio processing ──────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
soundfile>=0.12.1

# ── Server ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9   # required for FastAPI form parsing (Twilio webhook)
python-dotenv>=1.0.0

# ── LLM client (vLLM OpenAI-compatible endpoint) ─────────────────────────────
openai>=1.30.0

# ── System deps (install via apt, not pip) ────────────────────────────────────
# sudo apt install espeak-ng ffmpeg